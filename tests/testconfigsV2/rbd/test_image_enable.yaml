---
# test configuration info to enable image mirroring
configs:
  - method: enable
    class: Pool
    component: rbd_mirror
    args:
      pool_name: mirror_test
      mode: image
      site_name: site-a
    role: client
